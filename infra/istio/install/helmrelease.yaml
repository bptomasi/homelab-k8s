---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: base
  namespace: istio-system
spec:
  chart:
    spec:
      chart: ambient
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: istio-system
  interval: 24h0m0s
